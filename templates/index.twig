{# Params #}

{# Variables #}
{% set pageHerousel = entry.herousel.one|default(null) %}
{% set newsCards = craft.entries.section('news').orderBy('postDate asc').all %}

{% extends '_layout' %}

{# Markup #}
{% block content %}
  {% include 'components/_herousel' with {
    herousel: pageHerousel
  } %}

  {% include "components/_emerging-perspectives" with {
    cardData: newsCards
  } %}
{% endblock %}