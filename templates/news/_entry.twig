{# Params #}

{# Variables #}

{% extends '_layout' %}

{# Markup #}
{% block content %}
<article style='height: 75vh; width: 1200px; max-width: 90%; margin: 0 auto; display: flex; flex-direction: column; align-items: center; justify-content: center;'>
  <p>
    Entry Title: {{ entry.title }}
  </p>
  
  <p>
    {{ entry.copy }}
  </p>
</article>
{% endblock %}